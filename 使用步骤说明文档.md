# 使用步骤说明文档

本文档用于说明共享资料后，下一位同学需要先准备什么，再把内容粘贴到哪些文件的哪些位置。

## 一、先看总和

本次共享前，`douyinpachong-main` 文件夹以外，涉及隐私占位符、后续同学可能需要替换的文件一共 **3 个**：

1. `shiping-select-main\.env`
2. `shiping-select-main\项目推进记录\findings.md`
3. `shiping-select-main\项目推进记录\progress.md`

其中，真正运行项目时必须修改的是第 1 个文件 `.env`；第 2、3 个文件是项目推进记录中的代理端口记录，如果只是阅读资料，可以保持占位符不变。

另外，`shiping-select-main\.env.example` 和 `shiping-select-main\README.md` 中也有 API Key 示例，但它们本来就是说明用占位符，不需要填真实 Key。

> 注意：`douyinpachong-main` 文件夹本次按要求没有清理。如果要把这个文件夹也发给别人，需要另行检查其中的 Coze Token、本机路径和飞书 Webhook 配置。

## 二、第一步：申请 YouTube API Key

用途：用于 YouTube 搜索和视频元数据获取。

官方文档：

- YouTube Data API 入门：https://developers.google.com/youtube/v3/getting-started
- 获取授权凭据：https://developers.google.com/youtube/registering_an_application

申请步骤：

1. 打开 Google Cloud Console：https://console.cloud.google.com/
2. 登录自己的 Google 账号。
3. 创建一个新项目，项目名可以写 `video-resource-selector`。
4. 进入“API 和服务”。
5. 点击“启用 API 和服务”。
6. 搜索 `YouTube Data API v3`。
7. 点击进入后选择“启用”。
8. 回到“API 和服务”中的“凭据”页面。
9. 点击“创建凭据”。
10. 选择“API 密钥”。
11. 复制生成的 API Key。

粘贴位置：

文件夹：`shiping-select-main`

文件：`.env`

第 1 行：

```env
YOUTUBE_API_KEY=your_youtube_api_key
```

把第 1 行改成：

```env
YOUTUBE_API_KEY=你自己的YouTube_API_Key
```

注意事项：

- YouTube Data API 有每日 quota 限制。
- 不要把真实 API Key 发到群里或提交到公开仓库。
- 如果 API Key 泄露，应在 Google Cloud Console 里删除或重置。

## 三、第二步：申请 DeepSeek API Key

用途：用于大模型评估视频是否适合作为学习资源。

官方文档：

- DeepSeek API 文档：https://api-docs.deepseek.com/
- DeepSeek API 页面：https://api-docs.deepseek.com/api/deepseek-api

申请步骤：

1. 打开 DeepSeek 开放平台：https://platform.deepseek.com/
2. 注册或登录自己的账号。
3. 进入 API Keys / 密钥管理页面。
4. 创建新的 API Key。
5. 复制生成的 API Key。

粘贴位置：

文件夹：`shiping-select-main`

文件：`.env`

第 2 行：

```env
OPENAI_API_KEY=your_openai_compatible_api_key
```

把第 2 行改成：

```env
OPENAI_API_KEY=你自己的DeepSeek_API_Key
```

同一个 `.env` 文件里，第 3 行和第 4 行是模型接口配置：

```env
OPENAI_BASE_URL=https://api.deepseek.com
OPENAI_MODEL=deepseek-v4-pro
```

默认模型是 `deepseek-v4-pro`。如果继续使用 DeepSeek，并且想使用默认模型，这两行保持不变即可。

如果后续 DeepSeek 平台推荐使用其他模型，或者改用其他 OpenAI 兼容平台，则把第 4 行改成平台当前推荐的模型名，例如：

```env
OPENAI_MODEL=平台推荐的模型名
```

如果改用其他 OpenAI 兼容平台，还需要把第 3 行改成该平台提供的 Base URL。

注意事项：

- DeepSeek API Key 只显示一次，创建后要妥善保存。
- 不要把真实 API Key 写入 README、报告、截图或公开仓库。
- 如果怀疑泄露，应立即在平台后台删除或重置。

## 四、第三步：确认是否需要代理端口

如果需要访问 YouTube、Google API 或其他可能受网络限制的服务，通常需要本机代理。端口号要以自己电脑上的代理软件为准。

确认步骤：

1. 打开自己的代理软件，例如 Clash、Clash Verge、V2rayN、sing-box 图形客户端等。
2. 找到“端口”“HTTP 端口”“Mixed Port”“系统代理端口”等设置项。
3. 记录端口号，例如 `7890`、`7897`、`10809` 等。

注意：代理端口不是 API Key，也不是账号邮箱；它只是本机代理软件监听的端口号。

## 五、第四步：把代理端口粘贴到项目发现记录

这一步不是运行项目必须修改的内容。它只是为了让项目记录更符合下一位同学自己的电脑环境。

粘贴位置：

文件夹：`shiping-select-main\项目推进记录`

文件：`findings.md`

第 13 行：

```markdown
- YouTube 搜索最初使用 `google-api-python-client`，在代理环境下出现连接超时；改为 `httpx` 直接调用 YouTube REST API 后可通过 `<PROXY_PORT>` 代理成功搜索。
```

把 `<PROXY_PORT>` 替换成自己的代理端口。

例如，如果代理端口是 `7890`，则改成：

```markdown
- YouTube 搜索最初使用 `google-api-python-client`，在代理环境下出现连接超时；改为 `httpx` 直接调用 YouTube REST API 后可通过 `7890` 代理成功搜索。
```

如果只是阅读项目记录，不需要运行这条历史记录，可以不修改这一行。

## 六、第五步：把代理端口粘贴到项目进度记录

这一步同样不是运行项目必须修改的内容。它记录的是之前运行项目时使用过的代理命令。

粘贴位置：

文件夹：`shiping-select-main\项目推进记录`

文件：`progress.md`

约第 49 至 51 行：

```powershell
$env:HTTP_PROXY='http://127.0.0.1:<PROXY_PORT>'
$env:HTTPS_PROXY='http://127.0.0.1:<PROXY_PORT>'
$env:ALL_PROXY='http://127.0.0.1:<PROXY_PORT>'
```

把 `<PROXY_PORT>` 替换成自己的代理端口。

例如，如果代理端口是 `7890`，则改成：

```powershell
$env:HTTP_PROXY='http://127.0.0.1:7890'
$env:HTTPS_PROXY='http://127.0.0.1:7890'
$env:ALL_PROXY='http://127.0.0.1:7890'
```

如果不需要代理，可以不修改这个文件，运行命令时也不设置这些环境变量。

## 七、第六步：运行项目

进入主项目目录。把 `<你的资料保存路径>` 替换成自己电脑上保存资料的位置：

```powershell
cd "<你的资料保存路径>\shiping-select-main"
```

激活虚拟环境：

```powershell
.\.venv\Scripts\Activate.ps1
```

安装依赖：

```powershell
python -m pip install -r requirements.txt
```

运行测试：

```powershell
python -m pytest
```

运行 YouTube + LLM 示例：

```powershell
python main.py --topic "PCA 主成分分析" --platform youtube --max 15
```

如果需要通过代理运行，先设置代理：

```powershell
$env:HTTP_PROXY='http://127.0.0.1:你的代理端口'
$env:HTTPS_PROXY='http://127.0.0.1:你的代理端口'
$env:ALL_PROXY='http://127.0.0.1:你的代理端口'
python main.py --topic "PCA 主成分分析" --platform youtube --max 15
```

## 八、共享前检查

共享给其他同学前，建议确认：

1. `.env` 中没有真实 API Key。
2. 聊天截图或运行日志中没有真实 API Key。
3. 如果包含 `douyinpachong-main`，需要额外清理其中的 Coze Token、本机路径和飞书 Webhook 配置。
4. 不要上传 `.venv`、`.codex-venv`、`.pytest_cache`、`__pycache__` 这些运行缓存目录。
