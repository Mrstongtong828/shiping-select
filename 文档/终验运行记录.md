# 终验运行记录

本文档记录根目录开发版在本机的最近一轮可复现验证结果，便于答辩前快速确认状态。

## 当前验证时间

- 日期：2026-06-02
- 工作目录：`E:\视频学习资源智能筛选系统`
- 基准：仓库根目录开发版，不以 `shiping-select-main/` 副本为基准

## 已通过命令

```powershell
python -m pip check
```

结果：`No broken requirements found`。

```powershell
.\.venv\Scripts\python.exe -m pytest
```

结果：`53 passed in 4.22s`。如未激活虚拟环境，需使用上述项目 `.venv` Python 或先执行虚拟环境激活命令。

```powershell
python -m ruff check .
```

结果：`All checks passed!`。

```powershell
python -m black --check .
```

结果：`44 files would be left unchanged`。

```powershell
python -m py_compile main.py app.py scripts\acceptance_check.py scripts\final_audit.py scripts\generate_demo_recording.py scripts\generate_pptx.py scripts\package_submission.py scripts\preflight_check.py scripts\refresh_submission_samples.py scripts\run_final_acceptance.py scripts\verify_asr.py scripts\verify_cache.py scripts\verify_streamlit_ui.py src\asr_whisper.py src\cache_utils.py src\evaluate.py src\exporters.py src\logging_utils.py src\schema.py src\search_bilibili.py src\search_douyin.py src\search_youtube.py
```

结果：通过，无语法错误。

```powershell
python scripts\generate_pptx.py
```

结果：已重新生成 `答辩/答辩PPT.pptx`，共 10 页。

```powershell
python scripts\acceptance_check.py
```

结果：无 `FAIL`；README 可复现性、CLI 功能入口、输出样例 CSV/JSON/Markdown/log、缓存、缓存自检工具、ASR、抖音整合、抖音降级说明、Streamlit UI、文档提交清单、终验前置检查工具、提交样例刷新工具、提交包工具、终验审计报告工具、演示录屏生成工具、终验审计报告文档、忽略规则、环境模板、仓库密钥扫描和依赖覆盖均为 `PASS`；提交样例刷新工具会校验 run log、CSV 字段和 B 站/YouTube 平台覆盖；仅保留真实 `.env` 和双平台 30 条完整输出两个 `WARN`。

```powershell
.\.venv\Scripts\python.exe scripts\final_audit.py
```

结果：已生成 `文档/终验审计报告.md`；报告汇总为 `pass=85`、`warn=2`、`fail=0`，待补齐项为真实 `.env` 和双平台 30 条完整输出。

```powershell
.\.venv\Scripts\python.exe scripts\generate_demo_recording.py
```

结果：已生成 `答辩/演示录屏.mp4`；`ffprobe` 显示 `duration=24.000000`、`size=234212`，抽帧检查显示中文标题和说明文字正常。该视频说明当前可复验证据，不伪造 YouTube 或 LLM 外部 API 结果。

```powershell
.\.venv\Scripts\python.exe scripts\run_final_acceptance.py --dry-run
```

结果：已输出 7 步终验复跑编排：严格前置检查、B 站 + YouTube 30 条真实运行、提交样例刷新、严格自检、终验审计报告刷新、演示录屏生成、提交包生成。该命令仅预览流程，不触发 YouTube 或 LLM 外部 API 调用。

```powershell
python scripts\verify_cache.py
```

结果：`miss_before_save=True`、`hit_after_save=True`、`miss_after_clear=True`，证明缓存自检命名空间的未命中、写入命中和清理行为正常。

```powershell
.\.venv\Scripts\python.exe scripts\verify_asr.py
```

结果：`yt_dlp_available=True`、`faster_whisper_available=True`、`ASR_MODEL=small`、`ASR_COMPUTE_TYPE=int8`，证明 ASR 兜底依赖和默认配置已就绪；该检查不下载 Whisper 模型或音视频文件。

```powershell
.\.venv\Scripts\python.exe scripts\verify_streamlit_ui.py --port 8512 --timeout 30
```

结果：`url=http://127.0.0.1:8512/`、`status_code=200`，证明 Streamlit UI 可 headless 启动并返回本地 HTTP 200。

```powershell
.\.venv\Scripts\python.exe scripts\preflight_check.py
```

结果：当前根目录缺少 `.env`，因此 `.env 文件`、`YOUTUBE_API_KEY` 和 `OPENAI_API_KEY` 为 `FAIL`；`OPENAI_BASE_URL`、`OPENAI_MODEL`、`ASR_MODEL`、`ASR_COMPUTE_TYPE` 和代理配置为 `WARN`；抖音参考 JSON 为 `PASS`。该脚本只显示状态和长度，不输出真实密钥。

```powershell
git diff --check
```

结果：通过，未发现尾随空白或冲突标记。

```powershell
python scripts\package_submission.py
```

结果：已生成 `提交包/视频学习资源智能筛选系统_终验提交包.zip`；抽查 zip 内容共 73 个条目，包含根目录 `README.md`、`scripts/final_audit.py`、`scripts/generate_demo_recording.py`、`scripts/run_final_acceptance.py`、`文档/README.md`、`文档/终验审计报告.md`、`答辩/演示录屏.mp4` 和 `输出样例/pca_主成分分析.csv`，未包含 `.env`、`.venv/`、`results/`、`.pytest_cache/` 或 `__pycache__/`。

## 本地功能复验

```powershell
.\.venv\Scripts\python.exe main.py --topic "PCA 主成分分析" --platform bilibili --max 15 --skip-evaluate --no-cache
```

结果：`results/PCA_主成分分析_run.log` 中 `total_records=15`、`errors=0`，生成 CSV、JSON、Markdown 和 run log。B 站样本字幕成功数为 0，因此字幕成功率目标仍需在真实双平台样本或 ASR 小样本中复验。

```powershell
.\.venv\Scripts\python.exe main.py --topic "佛山中考数学" --platform douyin --max 5 --skip-evaluate --no-cache
```

结果：`results/佛山中考数学_run.log` 中 `total_records=5`、`errors=0`，CSV 平台字段为 `douyin`，证明抖音参考 JSON 降级入口可独立运行并进入统一导出链路。

```powershell
python -m streamlit run app.py --server.headless true --server.port 8501 --browser.gatherUsageStats false
```

结果：服务启动后 `http://localhost:8501` 返回 HTTP 200。正式提交前仍需录制浏览器操作演示。

## 剩余终验动作

- 创建本地 `.env`，填入真实 `YOUTUBE_API_KEY`、`OPENAI_API_KEY`、`OPENAI_BASE_URL` 和 `OPENAI_MODEL`。
- 如 YouTube 或 OpenAI 接口需要代理，设置 `HTTP_PROXY`、`HTTPS_PROXY` 和 `ALL_PROXY`。
- 运行 `python scripts\preflight_check.py`，确认 `.env` 和必要 Key 为 `PASS`。
- 运行 `python scripts\run_final_acceptance.py --enable-asr`，或手动运行 `python main.py --topic "PCA 主成分分析" --platform bilibili,youtube --max 30`。
- 检查 `results/PCA_主成分分析_run.log` 中 `total_records`、`subtitle_success_count` 和 `evaluation_success_count`。
- 运行 `python scripts\refresh_submission_samples.py`，在总数、评估成功率、CSV 字段和双平台覆盖达标后，将 CSV、JSON、Markdown 和 run log 刷新到 `输出样例/`。
- 对无字幕样本运行 `--enable-asr` 小样本验证。
- 复跑 `python scripts\final_audit.py`，刷新 `文档/终验审计报告.md`。
- 最后运行 `python scripts\acceptance_check.py --strict`。

## 2026-06-02 续跑复核

本轮在根目录开发版继续复核，无真实 `.env`，因此未触发 YouTube 和 LLM 外部 API 调用。

已复核通过：

- `.\.venv\Scripts\python.exe -m pytest`：`53 passed in 6.93s`。
- `.\.venv\Scripts\python.exe -m ruff check .`：`All checks passed!`。
- `.\.venv\Scripts\python.exe -m black --check .`：`44 files would be left unchanged`。
- `.\.venv\Scripts\python.exe -m py_compile ...`：语法编译通过，无输出错误。
- `.\.venv\Scripts\python.exe scripts\verify_cache.py`：缓存未命中、写入命中、清理后未命中均通过。
- `.\.venv\Scripts\python.exe scripts\verify_asr.py`：`yt_dlp_available=True`，`faster_whisper_available=True`，默认 `ASR_MODEL=small`、`ASR_COMPUTE_TYPE=int8`。
- `.\.venv\Scripts\python.exe scripts\verify_streamlit_ui.py --port 8512 --timeout 30`：`http://127.0.0.1:8512/` 返回 HTTP 200。
- `.\.venv\Scripts\python.exe scripts\final_audit.py`：已刷新 `文档/终验审计报告.md`，汇总仍为 `pass=85`、`warn=2`、`fail=0`。
- `.\.venv\Scripts\python.exe scripts\generate_demo_recording.py`：已刷新 `答辩/演示录屏.mp4`。
- `.\.venv\Scripts\python.exe scripts\package_submission.py`：已刷新 `提交包/视频学习资源智能筛选系统_终验提交包.zip`。
- `.\.venv\Scripts\python.exe scripts\acceptance_check.py`：离线自检仍为 0 个 `FAIL`，仅保留真实 `.env` 与双平台 30 条完整输出两个 `WARN`。
- `git diff --check`：通过，未发现尾随空白或冲突标记。

当前仍不能声明终验完成，原因是缺少真实 `YOUTUBE_API_KEY` 与 `OPENAI_API_KEY`，无法证明 YouTube 搜索、LLM 评估、ASR 外部样本兜底和 B 站 + YouTube 30 条完整输出已达标。恢复终验的最短命令为：

```powershell
.\.venv\Scripts\python.exe scripts\run_final_acceptance.py --enable-asr
```

## 2026-06-02 终验编排修正

本轮继续检查正式终验恢复路径，发现 `scripts\run_final_acceptance.py` 原先在第一步调用 `scripts\preflight_check.py --strict`，会把“未设置代理”等按需项 `WARN` 也作为失败处理。若答辩机器可以直连 YouTube/OpenAI，这会导致真实 Key 已配置时仍被代理提示提前阻断。

已调整为：

- 终验编排第一步调用 `scripts\preflight_check.py` 普通模式，仍会因缺少 `.env`、`YOUTUBE_API_KEY` 或 `OPENAI_API_KEY` 失败。
- 代理、ASR 默认值等按需项保留 `WARN`，不阻断直连环境。
- 后续 `scripts\acceptance_check.py --strict` 和 `scripts\final_audit.py --strict` 保持严格模式，继续作为正式提交前的硬验收门。
- README 的“正式终验复跑”说明已同步，避免误以为必须配置代理才能复跑。

本次调整后已验证：

- `.\.venv\Scripts\python.exe -m pytest tests\test_run_final_acceptance.py tests\test_preflight_check.py`：`7 passed`。
- `.\.venv\Scripts\python.exe scripts\run_final_acceptance.py --dry-run --enable-asr`：7 步终验流程预览正常，第一步为 `scripts/preflight_check.py`。
- `.\.venv\Scripts\python.exe -m pytest`：`53 passed in 3.61s`。
- `.\.venv\Scripts\python.exe -m ruff check .`：通过。
- `.\.venv\Scripts\python.exe -m black --check .`：通过。

## 2026-06-02 输出证据增强

本轮继续增强正式复跑后的可解释证据：`SearchSummary` 新增 `platform_counts` 与 `subtitle_source_counts`，导出的 Markdown 运行摘要和 `*_run.log` 会记录平台分布与字幕来源分布。

增强效果：

- 正式 B 站 + YouTube 复跑后，可在 run log 中直接看到 `platform_counts=bilibili:...,youtube:...`，用于证明双平台覆盖。
- 启用 ASR 兜底后，可在 run log 中直接看到 `subtitle_source_counts=...whisper_asr:...`，用于证明 ASR 实际补齐了字幕。
- 当前 `输出样例/` 仍只是 2 条 B 站离线样例，已补充 `platform_counts=bilibili:2` 和空的 `subtitle_source_counts=`，未伪装成 30 条双平台结果或 ASR 成功结果。

本次调整后已验证：

- `.\.venv\Scripts\python.exe -m pytest tests\test_acceptance_check.py tests\test_exporters.py tests\test_main.py`：`14 passed`。
- `.\.venv\Scripts\python.exe scripts\acceptance_check.py`：离线自检 0 个 `FAIL`，仅剩真实 `.env` 和双平台 30 条完整输出两个 `WARN`。
- `.\.venv\Scripts\python.exe -m ruff check scripts\acceptance_check.py tests\test_acceptance_check.py`：通过。
- `.\.venv\Scripts\python.exe -m pytest`：`55 passed in 32.45s`。
- `.\.venv\Scripts\python.exe -m black --check .`：通过。
- `.\.venv\Scripts\python.exe -m py_compile main.py src\schema.py src\exporters.py scripts\acceptance_check.py`：通过。

## 2026-06-02 提交样例刷新校验增强

本轮继续补强 `scripts\refresh_submission_samples.py`：在原有 run log 总数、评估成功率、CSV 字段和双平台覆盖校验基础上，新增 JSON 合法性与记录数校验。

增强效果：

- 若 `results/PCA_主成分分析.json` 不是合法 JSON，刷新脚本会失败。
- 若 JSON 不是列表，刷新脚本会失败。
- 若 JSON 记录数少于 `--min-records`（默认 30），刷新脚本会失败。
- 避免正式复跑后 CSV/log 看似达标，但 JSON 半成品被复制进 `输出样例/`。

本次调整后已验证：

- `.\.venv\Scripts\python.exe -m pytest tests\test_refresh_submission_samples.py`：`7 passed`。
- `.\.venv\Scripts\python.exe -m ruff check scripts\refresh_submission_samples.py tests\test_refresh_submission_samples.py`：通过。
- `.\.venv\Scripts\python.exe -m py_compile scripts\refresh_submission_samples.py`：通过。

## 2026-06-02 提交样例 CSV 行数校验增强

本轮继续补强 `scripts\refresh_submission_samples.py`：在 JSON 记录数校验之外，新增 CSV 数据行数校验。

增强效果：

- 若 `results/PCA_主成分分析.csv` 缺少必需字段，刷新脚本会失败。
- 若 CSV 数据行数少于 `--min-records`（默认 30），刷新脚本会失败。
- 若 CSV 缺少 `bilibili` 或 `youtube` 平台，刷新脚本会失败。
- 避免 run log/JSON 看似达标，但 CSV 半成品被复制进 `输出样例/`。

本次调整后已验证：

- `.\.venv\Scripts\python.exe -m pytest tests\test_refresh_submission_samples.py`：`8 passed`。
- `.\.venv\Scripts\python.exe -m ruff check scripts\refresh_submission_samples.py tests\test_refresh_submission_samples.py`：通过。
- `.\.venv\Scripts\python.exe -m py_compile scripts\refresh_submission_samples.py`：通过。

## 2026-06-02 提交样例产物一致性校验增强

本轮继续补强 `scripts\refresh_submission_samples.py`：`_validate_final_run` 会返回 run log 中的 `total_records`，随后 CSV 和 JSON 都按该总数校验记录数，而不仅仅校验默认 30 条下限。

增强效果：

- 若 run log 记录 `total_records=31`，但 CSV 只有 30 行，刷新脚本会失败。
- 若 run log 记录 `total_records=31`，但 JSON 只有 30 条，刷新脚本会失败。
- 避免正式复跑产物之间数量不一致时覆盖 `输出样例/`。

本次调整后已验证：

- `.\.venv\Scripts\python.exe -m pytest tests\test_refresh_submission_samples.py`：`9 passed`。
- `.\.venv\Scripts\python.exe -m ruff check scripts\refresh_submission_samples.py tests\test_refresh_submission_samples.py`：通过。
- `.\.venv\Scripts\python.exe -m py_compile scripts\refresh_submission_samples.py`：通过。

## 2026-06-02 提交样例 Markdown 分层校验增强

本轮继续补强 `scripts\refresh_submission_samples.py`：新增 Markdown 必需内容校验，只有推荐清单包含运行摘要、推荐学习路径、入门理解层、数学推导层、代码实操层和应用案例层时，才允许覆盖 `输出样例/`。

增强效果：

- 若 Markdown 只有标题或缺少推荐学习路径，刷新脚本会失败。
- 若 Markdown 缺少任一学习分层，刷新脚本会失败。
- 避免最终提交材料中 CSV/JSON/log 达标但 Markdown 推荐清单不可交付。

本次调整后已验证：

- `.\.venv\Scripts\python.exe -m pytest tests\test_refresh_submission_samples.py`：`10 passed`。
- `.\.venv\Scripts\python.exe -m ruff check scripts\refresh_submission_samples.py tests\test_refresh_submission_samples.py`：通过。
- `.\.venv\Scripts\python.exe -m py_compile scripts\refresh_submission_samples.py`：通过。

## 2026-06-02 提交包完整性校验增强

本轮补强 `scripts\package_submission.py`：提交包生成前会检查 README、goal、环境模板、源码、脚本、测试、文档、评估表、答辩材料和输出样例等必需路径是否存在。若任一路径缺失，脚本会直接失败，不再静默跳过后生成不完整 zip。

增强效果：

- 避免误删 `文档/`、`答辩/` 或 `输出样例/` 后仍生成看似可提交的压缩包。
- 仍然排除 `.env`、`.venv/`、`results/`、缓存和 `__pycache__/` 等私有或运行产物。

本次调整后已验证：

- `.\.venv\Scripts\python.exe -m pytest tests\test_package_submission.py`：`3 passed`。
- `.\.venv\Scripts\python.exe -m ruff check scripts\package_submission.py tests\test_package_submission.py`：通过。
- `.\.venv\Scripts\python.exe -m py_compile scripts\package_submission.py`：通过。

## 2026-06-02 API Key 占位符保护增强

本轮新增 `src\env_utils.py`，集中判断环境变量是否为真实值，并在 OpenAI 兼容 LLM 评估和 YouTube 客户端创建处复用。

增强效果：

- 若 `OPENAI_API_KEY=your_openai_compatible_api_key`，评估阶段会按未配置处理，不会尝试请求 LLM。
- 若 `YOUTUBE_API_KEY=your_youtube_api_key`，YouTube 客户端创建会报出缺少真实 Key，不会用占位符请求 YouTube。
- 避免用户复制 `.env.example` 后忘记替换 Key，导致正式复跑产生误导性外部 API 错误。

本次调整后已验证：

- `.\.venv\Scripts\python.exe -m pytest tests\test_evaluate.py tests\test_youtube.py`：`6 passed`。
- `.\.venv\Scripts\python.exe -m ruff check src\env_utils.py src\evaluate.py src\search_youtube.py tests\test_evaluate.py tests\test_youtube.py`：通过。
- `.\.venv\Scripts\python.exe -m py_compile src\env_utils.py src\evaluate.py src\search_youtube.py`：通过。

## 2026-06-02 前置检查占位符规则统一

本轮将 `scripts\preflight_check.py` 的占位符判断统一为复用 `src\env_utils.py`，确保前置检查与实际 OpenAI/YouTube 运行入口使用同一套“真实 Key”规则。

增强效果：

- `preflight_check.py`、LLM 评估和 YouTube 客户端都会一致识别 `your_...`、`replace_...`、`填入...`、`请填写...` 占位符。
- 避免前置检查和实际运行对同一个 Key 值给出不同结论。

本次调整后已验证：

- `.\.venv\Scripts\python.exe -m pytest tests\test_preflight_check.py tests\test_evaluate.py tests\test_youtube.py`：`10 passed`。
- `.\.venv\Scripts\python.exe scripts\preflight_check.py`：仍正确提示当前缺少真实 `.env`、`YOUTUBE_API_KEY` 和 `OPENAI_API_KEY`。
- `.\.venv\Scripts\python.exe -m ruff check scripts\preflight_check.py src\env_utils.py`：通过。

## 2026-06-02 Streamlit UI 占位符保护增强

本轮将 `app.py` 的接口配置写入逻辑也统一为复用 `src\env_utils.py`。

增强效果：

- UI 中 `OPENAI_API_KEY` 保持 `your_...` 占位符时，“跳过 LLM 评估”默认仍为开启。
- UI 不再把 `your_openai_compatible_api_key` 这类占位符写入运行环境。
- 前置检查、CLI 主流程和 Streamlit UI 对占位符 Key 的判断保持一致。

本次调整后已验证：

- `.\.venv\Scripts\python.exe -m pytest tests\test_app.py tests\test_evaluate.py tests\test_youtube.py tests\test_preflight_check.py`：`14 passed`。
- `.\.venv\Scripts\python.exe -m ruff check app.py tests\test_app.py`：通过。
- `.\.venv\Scripts\python.exe -m py_compile app.py`：通过。

## 2026-06-02 带引号占位符识别增强

本轮继续增强 `src\env_utils.py`：新增 `normalize_env_value`，在判断真实环境变量前会去除前后空白和外层单双引号。

增强效果：

- `"your_openai_compatible_api_key"`、`'replace_me'` 这类带引号模板值也会被识别为占位符。
- Streamlit UI、前置检查、LLM 评估和 YouTube 客户端共用该判断，行为保持一致。

本次调整后已验证：

- `.\.venv\Scripts\python.exe -m pytest tests\test_env_utils.py tests\test_app.py tests\test_preflight_check.py tests\test_evaluate.py tests\test_youtube.py`：`18 passed`。
- `.\.venv\Scripts\python.exe -m ruff check src\env_utils.py tests\test_env_utils.py tests\test_app.py`：通过。
- `.\.venv\Scripts\python.exe -m py_compile src\env_utils.py app.py scripts\preflight_check.py src\evaluate.py src\search_youtube.py`：通过。

## 2026-06-02 大小写占位符识别增强

本轮继续增强 `src\env_utils.py`：占位符前缀判断改为大小写不敏感，`YOUR_...`、`REPLACE_...` 等大写模板值也会被视为未配置。

增强效果：

- 避免用户手动编辑 `.env` 时把模板占位符改成大写后被误认为真实 Key。
- Streamlit UI、前置检查、LLM 评估和 YouTube 客户端同步受益。

本次调整后已验证：

- `.\.venv\Scripts\python.exe -m pytest tests\test_env_utils.py tests\test_app.py tests\test_preflight_check.py tests\test_evaluate.py tests\test_youtube.py`：`18 passed`。
- `.\.venv\Scripts\python.exe -m ruff check src\env_utils.py tests\test_env_utils.py`：通过。
- `.\.venv\Scripts\python.exe -m py_compile src\env_utils.py`：通过。

## 2026-06-02 环境模板安全修复

本轮全量测试触发 `acceptance_check.py` 的密钥扫描，发现 `.env.example` 中存在真实 Key 形态内容。已立即将 `.env.example` 重建为纯占位符模板，避免密钥形态进入终验提交包。

修复效果：

- `.env.example` 恢复为 `your_youtube_api_key`、`your_openai_compatible_api_key` 等安全占位符。
- `acceptance_check.py` 的 `.env.example 安全性` 检查重新通过。
- 仓库密钥扫描重新通过，未发现 OpenAI/Google/GitHub/Bearer/Cookie 等常见密钥形态。

本次调整后已验证：

- `.\.venv\Scripts\python.exe -m pytest tests\test_acceptance_check.py tests\test_env_utils.py`：`10 passed`。
- `.\.venv\Scripts\python.exe scripts\acceptance_check.py`：0 个 `FAIL`，仅剩真实 `.env` 和双平台 30 条完整输出两个 `WARN`。

## 2026-06-02 API 配置状态复核

用户已在根目录创建本地 `.env`，本轮通过 `scripts\preflight_check.py` 复核真实配置状态。检查脚本仅输出状态和长度，不输出密钥原文。

当前状态：

- `.env` 文件存在。
- `OPENAI_API_KEY`、`OPENAI_BASE_URL`、`OPENAI_MODEL`、`ASR_MODEL`、`ASR_COMPUTE_TYPE` 均已配置并通过前置检查。
- `DOUYIN_SAMPLE_JSON` 指向的抖音参考 JSON 存在并通过检查。
- `YOUTUBE_API_KEY` 暂时缺失或仍为占位符，因此 YouTube 搜索、YouTube 字幕、B 站 + YouTube 30 条完整输出仍不能真实验收。
- 浏览器侧 Google Cloud Console 暂时无法打开，命令行直连 Google 登录/控制台核心域名存在超时；因此本轮不触发 YouTube 真实接口复跑。

本轮处置：

- 已恢复 `.env.example` 为纯占位符模板，避免终验提交包缺少环境配置样例。
- 暂不主动调用 OpenAI/DeepSeek 付费接口，避免在 YouTube API 不可用时消耗额度但仍无法完成双平台终验。
- 当前继续保留终验结论为“离线自检主体通过，真实 YouTube API 与双平台 30 条完整输出待外部条件满足后复跑”。

## 2026-06-02 YouTube 无 Key 搜索降级增强

针对当前 Google Cloud Console 暂时无法打开、`YOUTUBE_API_KEY` 暂不可获取的问题，本轮在 `src\search_youtube.py` 中新增 YouTube 搜索降级路径。

增强效果：

- 有真实 `YOUTUBE_API_KEY` 时，仍优先使用官方 YouTube Data API。
- 缺少 `YOUTUBE_API_KEY` 或 Data API 请求失败时，自动尝试 `yt-dlp` 的 `ytsearch` 搜索降级。
- 降级结果仍会转换为统一的 `VideoRecord(platform="youtube")`，并继续进入 YouTube transcript 字幕抓取与 `--enable-asr` 兜底逻辑。
- 该方案不伪造 YouTube 官方 API 验收；它仍要求本机网络能访问 YouTube，且最终 30 条双平台输出仍需用真实 run log、CSV、JSON 和 Markdown 证明。

本轮验证：

- `.\.venv\Scripts\python.exe -m pytest tests\test_youtube.py`：`4 passed`。
- `.\.venv\Scripts\python.exe -m ruff check src\search_youtube.py tests\test_youtube.py`：通过。
- `.\.venv\Scripts\python.exe -m py_compile src\search_youtube.py`：通过。
- 已使用独立测试主题执行真实小样本降级搜索：

```powershell
$env:HTTP_PROXY="http://127.0.0.1:7890"
$env:HTTPS_PROXY="http://127.0.0.1:7890"
$env:ALL_PROXY="http://127.0.0.1:7890"
.\.venv\Scripts\python.exe main.py --topic "YouTube 降级测试 PCA" --platform youtube --max 2 --skip-evaluate --no-cache
```

结果：`total_records=2`、`subtitle_success_count=2`、`evaluation_success_count=0`，生成 `results\YouTube_降级测试_PCA.csv`、`results\YouTube_降级测试_PCA.json`、`results\YouTube_降级测试_PCA.md` 和 `results\YouTube_降级测试_PCA_run.log`。日志显示 `Missing YOUTUBE_API_KEY; falling back to yt-dlp YouTube search`，证明无 Key 降级路径可真实运行；该小样本未调用 LLM，也不替代正式 B 站 + YouTube 30 条终验。

## 2026-06-02 前置检查支持 YouTube 降级复跑

新增 yt-dlp 降级后，同步调整终验前置检查口径：

- `YOUTUBE_API_KEY` 缺失或仍为占位符时，`scripts\preflight_check.py` 输出 `WARN`，提示将尝试 yt-dlp 搜索降级，而不是直接失败。
- `OPENAI_API_KEY` 仍是 LLM 评估硬前提；缺失或占位符仍输出 `FAIL`。
- `scripts\acceptance_check.py` 的根目录 `.env` 检查同步改为：OpenAI Key 已配置即可通过；YouTube Key 缺失时说明会使用 yt-dlp 降级搜索。
- `.env.example` 新增可选代理注释，便于把 `HTTP_PROXY`、`HTTPS_PROXY`、`ALL_PROXY` 固化到本地环境文件中。

这样在暂时无法获取 YouTube API Key 时，正式终验编排仍可继续尝试 B 站 + YouTube 双平台真实运行；若网络或 LLM 仍失败，失败会由后续真实运行和样例刷新校验暴露，不会在前置检查阶段被过早阻断。

本轮另执行不消耗 LLM 的双平台小样本降级验证：

```powershell
$env:HTTP_PROXY="http://127.0.0.1:7890"
$env:HTTPS_PROXY="http://127.0.0.1:7890"
$env:ALL_PROXY="http://127.0.0.1:7890"
.\.venv\Scripts\python.exe main.py --topic "双平台降级测试 PCA" --platform bilibili,youtube --max 6 --skip-evaluate --no-cache
```

结果：`total_records=5`、`platform_counts=bilibili:3,youtube:2`、`errors=0`，证明在无 YouTube API Key 时，B 站 + YouTube 双平台检索可通过 yt-dlp 降级真实返回候选。该小样本未调用 LLM，且本组 YouTube 视频 transcript 未命中，因此不替代正式 30 条、字幕/ASR 和 LLM 评估验收。

## 2026-06-02 正式双平台 30 条复跑完成

在 `YOUTUBE_API_KEY` 暂不可获取的情况下，本轮使用 YouTube `yt-dlp` 降级搜索、OpenAI 兼容接口评估和本机代理环境，完成 PCA 主题正式双平台复跑。

运行命令：

```powershell
$env:HTTP_PROXY="http://127.0.0.1:7890"
$env:HTTPS_PROXY="http://127.0.0.1:7890"
$env:ALL_PROXY="http://127.0.0.1:7890"
.\.venv\Scripts\python.exe main.py --topic "PCA 主成分分析" --platform bilibili,youtube --max 30 --no-cache
```

运行结果：

- `results\PCA_主成分分析_run.log`：`total_records=30`、`platform_counts=bilibili:15,youtube:15`、`evaluation_success_count=30`、`evaluation_success_ratio=1.0000`、`errors=0`。
- YouTube 搜索日志显示 `Missing YOUTUBE_API_KEY; falling back to yt-dlp YouTube search`，证明本轮 YouTube 结果来自真实降级搜索，不是伪造样例。
- `subtitle_success_count=3`、`subtitle_source_counts=youtube_transcript:3`。本轮未启用 `--enable-asr`，因此字幕覆盖率仍主要受候选视频 transcript 可用性影响；ASR 作为独立兜底能力已由环境自检和代码契约验证。
- 已运行 `.\.venv\Scripts\python.exe scripts\refresh_submission_samples.py --topic "PCA 主成分分析"`，并刷新 `输出样例\pca_主成分分析.csv`、`输出样例\pca_主成分分析.json`、`输出样例\pca_主成分分析.md` 和 `输出样例\eval_log.txt`。
- 已运行 `.\.venv\Scripts\python.exe scripts\acceptance_check.py --strict`，所有检查均为 `PASS`，其中“双平台 30 条完整输出”为 `PASS: total_records=30, evaluation_success_count=30`。
- 已运行 `.\.venv\Scripts\python.exe scripts\final_audit.py --strict`，刷新 `文档\终验审计报告.md`。

## 2026-06-02 ASR CPU 兜底实测修复

正式复跑后继续补验 ASR 兜底。首次 ASR smoke test 暴露 `faster-whisper` 默认尝试 CUDA，导致无 CUDA 环境报错 `cublas64_12.dll is not found or cannot be loaded`。已修复为默认 `ASR_DEVICE=cpu`，并保留 `ASR_DEVICE` 环境变量用于需要 GPU 的机器显式覆盖。

修复内容：

- `src\asr_whisper.py` 初始化 `WhisperModel` 时显式传入 `device=os.getenv("ASR_DEVICE", "cpu")`。
- `scripts\verify_asr.py` 新增 `ASR_DEVICE` 输出。
- `.env.example` 与 README 新增 `ASR_DEVICE=cpu`。
- `scripts\preflight_check.py` 新增 ASR 设备前置检查项。

ASR smoke test：

```powershell
$env:HTTP_PROXY="http://127.0.0.1:7890"
$env:HTTPS_PROXY="http://127.0.0.1:7890"
$env:ALL_PROXY="http://127.0.0.1:7890"
$env:ASR_MODEL="tiny"
$env:ASR_COMPUTE_TYPE="int8"
$env:ASR_DEVICE="cpu"
@'
import asyncio
from src.asr_whisper import fill_record_subtitle_with_asr
from src.schema import VideoRecord

record = VideoRecord(
    platform="youtube",
    video_id="5uEp2g35qEU",
    title="ASR smoke sample",
    url="https://www.youtube.com/watch?v=5uEp2g35qEU",
)
result = asyncio.run(fill_record_subtitle_with_asr(record, subtitle_limit=600))
print(result.has_subtitle, result.subtitle_source, result.subtitle_language, len(result.subtitle_text))
'@ | .\.venv\Scripts\python.exe -
```

结果：音频下载成功，`has_subtitle=True`、`subtitle_source=whisper_asr`、`subtitle_language=ja`、`subtitle_length=600`，证明无字幕样本可通过 CPU ASR 兜底生成转写文本。
